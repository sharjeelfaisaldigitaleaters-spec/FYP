fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
supabase>=2.3.0
groq>=0.4.0
elevenlabs>=1.0.2
# Speech-to-text now runs via Groq's hosted Whisper API (see stt_engine.py) —
# faster-whisper was a local model that, combined with the embedder, pushed
# memory-constrained hosts over the edge mid-request.
# fastembed uses ONNX Runtime, not torch/sentence-transformers — the same
# 384-dim embedding quality at a fraction of the memory (no torch/transformers
# import needed), which is what makes running comfortably in 512MB possible.
fastembed>=0.4.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0
numpy>=1.26.0
httpx>=0.27.0
email-validator>=2.0.0
pypdf>=4.0.0


